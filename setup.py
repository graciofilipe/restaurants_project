from setuptools import setup, find_packages

setup(
    name='common_utils',
    version='0.1.0',
    packages=find_packages(),
    description='Shared utility functions for the restaurant project',
    # Add other metadata as needed (author, license, etc.)
)
